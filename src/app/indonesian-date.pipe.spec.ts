import { IndonesianDatePipe } from './indonesian-date.pipe';

describe('IndonesianDatePipe', () => {
  it('create an instance', () => {
    const pipe = new IndonesianDatePipe();
    expect(pipe).toBeTruthy();
  });
});
