import { IndonesianCurrencyPipe } from './indonesian-currency.pipe';

describe('IndonesianCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new IndonesianCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
